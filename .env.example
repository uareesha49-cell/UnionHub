# Production / Netlify: browser must call a real API host (see netlify.toml).
# Set in Netlify → Site configuration → Environment variables, then redeploy.
# Example: https://your-backend.vercel.app
VITE_API_ORIGIN=

# Local: match backend PORT — see backend/.env.example
# VITE_DEV_API_PORT=4000
